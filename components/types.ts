export type Item = {
  id: string;
  reportId: string;   
  item: string;       
  category: string;
  description: string;
  location: string;
  date: string;
  imageUrl?: string;
  userId: string;
  type: string;
};